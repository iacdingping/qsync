<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">


<!-- namespace必须指向Dao接口 -->
<mapper namespace="com.openteach.qsync.core.dao.order.OrderStatusDao">
	
	<!-- 用于select查询公用抽取的列 -->
	<sql id="order-OrderStatus-columns">
	    <![CDATA[
		`id` as `id`, `status` as `status`, `order_id` as `orderId`, `creatoradmin` as `creatoradmin`, `createdatetime` as `createdatetime`
	    ]]>
	</sql>	

	<!-- useGeneratedKeys="true" keyProperty="xxx" for sqlserver and mysql -->
	<insert id="save" useGeneratedKeys="true" keyProperty="id">
    <![CDATA[
        INSERT INTO cc_kata_kplus_order_status (
        `id`, `status`, `order_id`, `creatoradmin`, `createdatetime`
        ) VALUES (
        #{id}, #{status}, #{orderId}, #{creatoradmin}, #{createdatetime}        
        )
    ]]>
		<!--	
			oracle: order="BEFORE" SELECT sequenceName.nextval AS ID FROM DUAL 
			DB2: order="BEFORE"" values nextval for sequenceName
		<selectKey resultType="java.lang.Long" order="BEFORE" keyProperty="userId">
			SELECT sequenceName.nextval AS ID FROM DUAL 
        </selectKey>
		-->
	</insert>
    
	<update id="update">
    <![CDATA[
        UPDATE cc_kata_kplus_order_status SET
	        `status` = #{status} , `order_id` = #{orderId} , `creatoradmin` = #{creatoradmin} , `createdatetime` = #{createdatetime}  
        WHERE 
        	id = #{id}  	        
    ]]>
	</update>

    <delete id="deleteById">
    <![CDATA[
        DELETE FROM cc_kata_kplus_order_status WHERE
        `id` = #{id} 
    ]]>
    </delete>
    
    <select id="getById" resultType="OrderStatus">
		SELECT <include refid="order-OrderStatus-columns" />
	    <![CDATA[
		    FROM cc_kata_kplus_order_status 
	        WHERE 
		        `id` = #{id} 
	    ]]>
	</select>
	
	<sql id="OrderStatus-where">
		<!-- ognl访问静态方法的表达式 为@class@method(args),以下为调用rapid中的Ognl.isNotEmpty()方法,还有其它方法如isNotBlank()可以使用，具体请查看Ognl类 -->
		<where>	      				
	       <if test="id">
				AND `id` = #{id}
			</if>
	       <if test="status">
				AND `status` = #{status}
			</if>
	       <if test="orderId">
				AND `order_id` = #{orderId}
			</if>
	       <if test="creatoradmin">
				AND `creatoradmin` = #{creatoradmin}
			</if>
	       <if test="createdatetimeBegin != null">
				AND `createdatetime` >= #{createdatetimeBegin}
		   </if>
		   <if test="createdatetimeEnd != null">
				AND `createdatetime` &lt;= #{createdatetimeEnd}
		   </if>
		</where>
	</sql>
		
    <select id="count" resultType="long">
        SELECT count(*) FROM cc_kata_kplus_order_status 
		<include refid="OrderStatus-where"/>    
    </select>
    
    <!--
    	分页查询已经使用Dialect进行分页,也可以不使用Dialect直接编写分页
    	因为分页查询将传 offset,pageSize,lastRows 三个参数,不同的数据库可以根于此三个参数属性应用不同的分页实现
    -->
    <select id="findPage" resultType="OrderStatus">
    	SELECT <include refid="order-OrderStatus-columns" />
	    FROM cc_kata_kplus_order_status 
		<include refid="OrderStatus-where"/>
		
		<if test="sortColumns != null">
			ORDER BY ${sortColumns}
		</if>
    </select>
	
	<select id="list" resultType="OrderStatus">
    	SELECT <include refid="order-OrderStatus-columns" />
	    FROM cc_kata_kplus_order_status 
		<include refid="OrderStatus-where"/>
		
		<if test="sortColumns != null">
			ORDER BY ${sortColumns}
		</if>
    </select>

	
</mapper>

